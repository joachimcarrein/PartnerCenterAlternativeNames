Please update the tenant domain title to "Alternative Name", also update the app to be named "Partner Center Alternative Names" 

If you feel like this should get a more appropriate icon, do so.

Make the "alternative name" field editable and allow the user to overwrite it with a custom name if the user so chooses